{
  "name": "repo-sync",
  "version": "1.0.0",
  "description": "Action to sync one repository branch, with another",
  "main": "dist/parseGithubUrl/index.js",
  "scripts": {
    "test": "jest",
    "build": "npx ts-node buildScript.ts"
  },
  "author": "Arky Asmal",
  "license": "ISC",
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "esbuild": "^0.17.4"
  },
  "dependencies": {
    "octokit": "^3.1.1"
  }
}
